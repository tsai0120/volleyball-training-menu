# 📝 数据管理指南

## ⚠️ 当前问题

**为什么不同设备看不到变更？**

目前系统使用 `localStorage` 存储数据，这是**本地浏览器存储**，特点：
- ✅ 只在当前设备/浏览器有效
- ❌ 不同设备之间**不会同步**
- ❌ 清除浏览器缓存会丢失数据
- ❌ 图片上传后只存在当前设备

## 🎯 解决方案

### 方案一：修改初始数据文件（推荐用于固定内容）

**适用场景**：训练菜单、出席人员等固定内容

**修改位置**：`lib/data.ts`

#### 如何修改训练菜单：

1. **打开文件**：`lib/data.ts`

2. **找到要修改的项目**，例如：
```typescript
{
  id: 'p1-1',
  title: '全場接嗆死',
  duration: 20,
  category: 'practice',
  requirements: '20顆*2趟',
  rounds: 2,
  description: '舉球順便練舉',
  startTime: '12:40',
  imageUrl: 'https://example.com/image.jpg', // 添加图片URL
}
```

3. **添加图片**：
   - 将图片放到 `public/images/` 文件夹
   - 在 `imageUrl` 中使用相对路径：`/images/your-image.jpg`
   - 或者使用外部图片URL

4. **保存并推送**：
```bash
git add lib/data.ts
git commit -m "Update training menu"
git push
```

5. **Vercel 会自动重新部署**，所有设备都会看到更新！

#### 如何修改出席人员：

在 `lib/data.ts` 中找到 `attendanceData` 数组，修改或添加：

```typescript
export const attendanceData: Attendance[] = [
  { grade: '大一', name: '劉冠妤', days: [true, true, true] },
  { grade: '大一', name: '新成員', days: [true, false, false] }, // 新增
  // ...
]
```

---

### 方案二：使用 Public 文件夹存储图片（推荐）

**适用场景**：训练项目的图片说明

#### 步骤：

1. **创建图片文件夹**：
```bash
mkdir -p public/images/training
```

2. **添加图片**：
   - 将图片文件放到 `public/images/training/` 文件夹
   - 例如：`public/images/training/warmup-ball.jpg`

3. **在代码中引用**：
```typescript
{
  id: 'w1-1',
  title: '球操（舉球拉開）',
  imageUrl: '/images/training/warmup-ball.jpg', // 使用相对路径
}
```

4. **推送代码和图片**：
```bash
git add public/images/
git add lib/data.ts
git commit -m "Add training images"
git push
```

5. **所有设备都能看到图片！**

---

### 方案三：清除本地存储（重置为初始数据）

如果某台设备的数据混乱了，可以：

1. **打开浏览器开发者工具**（F12 或 Cmd+Option+I）
2. **Application 标签页** → **Local Storage**
3. **删除以下键**：
   - `volleyball_training_days`
   - `volleyball_attendance`
4. **刷新页面**，会重新加载 `lib/data.ts` 的初始数据

---

## 📂 文件结构说明

```
寒訓菜單/
├── lib/
│   └── data.ts          ← 初始数据文件（所有设备共享）
├── public/
│   └── images/          ← 图片文件夹（所有设备共享）
│       └── training/    ← 训练项目图片
├── hooks/
│   └── useTrainingData.ts  ← 数据管理 Hook
└── lib/
    └── storage.ts       ← localStorage 存储逻辑
```

---

## 🔄 工作流程建议

### 对于固定内容（训练菜单、出席人员）：

1. **在代码中修改** `lib/data.ts`
2. **推送代码**到 GitHub
3. **Vercel 自动部署**
4. **所有设备自动更新**

### 对于临时变更（测试、临时调整）：

1. **在网页上直接编辑**（使用管理员功能）
2. **数据存在 localStorage**（仅当前设备）
3. **如需同步，导出数据并更新 `lib/data.ts`**

---

## 💡 最佳实践

1. **图片管理**：
   - 使用 `public/images/` 文件夹
   - 文件名使用英文和数字（避免中文文件名问题）
   - 图片大小建议 < 500KB

2. **数据更新**：
   - 重要变更 → 修改 `lib/data.ts` → 推送代码
   - 临时测试 → 使用网页编辑 → 仅当前设备

3. **版本控制**：
   - 每次修改 `lib/data.ts` 都要 commit 和 push
   - 这样所有设备都能同步

---

## 🚀 快速操作指南

### 添加训练项目图片：

```bash
# 1. 添加图片到 public 文件夹
cp ~/Downloads/training-image.jpg public/images/training/

# 2. 修改 lib/data.ts，添加 imageUrl
# 找到对应项目，添加：
# imageUrl: '/images/training/training-image.jpg'

# 3. 推送
git add public/images/ lib/data.ts
git commit -m "Add training image"
git push
```

### 更新出席人员：

```bash
# 1. 编辑 lib/data.ts
# 修改 attendanceData 数组

# 2. 推送
git add lib/data.ts
git commit -m "Update attendance"
git push
```

---

## ❓ 常见问题

**Q: 为什么我上传的图片在其他设备看不到？**
A: 因为图片存在 localStorage（base64），只存在当前设备。应该使用 `public/images/` 文件夹。

**Q: 如何让所有设备看到我的修改？**
A: 修改 `lib/data.ts` 文件，然后 `git push`，Vercel 会自动部署。

**Q: 可以删除 localStorage 的数据吗？**
A: 可以，在浏览器开发者工具中删除，页面会重新加载初始数据。

**Q: 图片太大怎么办？**
A: 使用图片压缩工具（如 TinyPNG）压缩后再上传。

